#include "lists.h"

/**
 * check_cycle - checks a cycle
 * @list: list
 *
 * Returns: 0 if there's a cycle, 1 if not
 */

int check_cycle(listint_t *list)
{
	int n = 0;

	if (list)
		n = 1;
	return (n);
}
